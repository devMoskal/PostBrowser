package com.dev.moskal.postbrowser.app.postdetail

/**
 * Listener used to process user interaction with [DetailsListItem.AlbumItem]
 */
interface AlbumClickListener {
    fun onClick(album: DetailsListItem.AlbumItem)
}